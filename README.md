# Simple_Offline_Database
A simple local database to get started with SQLlite DB . The values are taken in via mobile application and stored in local database with gets destroyed on closing of application.  
